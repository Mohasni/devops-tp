apiVersion: v1
kind: Service
metadata:
  name: mon-app-js
  labels:
    app: mon-app-js
spec:
  type: NodePort
  selector:
    app: mon-app-js
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
    protocol: TCP
    name: http